#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int main() {
	ios_base:: sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int count = 0;
	for (int i = 1; i <= 28; i++) {
		double x;
		cin >> x;
		if (x >= 0.8) {
			count++;
		}
	}
	cout << count;
	return 0;
}
